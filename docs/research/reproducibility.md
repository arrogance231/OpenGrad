# Reproducibility

Freeze Git SHA, model revision, dataset manifest and cryptographic hash, benchmark/evaluator versions, seed, hardware, software, runtime, compute provider, generation settings, and artifact locations. Record unavailable fields as null or UNKNOWN, never by inference. CI uses tiny synthetic fixtures and never downloads checkpoints or requires GPUs.
