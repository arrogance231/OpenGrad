# OpenWeights boundary

OpenWeights is a downstream environment, not a dependency and was not modified. Future flow: accepted OpenGrad checkpoint → HF/Safetensors, GGUF → llama.cpp/OpenWeights, or ExecuTorch `.pte` → device evaluation. `registry/interop.schema.json` defines the future result envelope.
